class utilsChronvs {
  constructor(tela) {
    this.tela = tela;
    this.main = {};
  }
  clearScreen() {
    D4.set();
    this.tela.clear();
  }
  setMain(main) {
    this.main = main;
  }
  getMain() {
    return this.main;
  }
  drwStr(valor, x, y) {
    this.tela.drawString(this.tela.wrapString(valor, this.tela.getWidth()).join("\n"), x, y);
  }
}
exports = utilsChronvs